find_package(Node QUIET)
find_package(ZeroMQPP QUIET)

if(Node_FOUND AND ZeroMQPP_FOUND)
    set( BUILD_NodeCam false CACHE BOOL force )
    if( BUILD_NodeCam )
        message( STATUS "HAL: building 'NodeCam' camera driver.")
        add_to_hal_sources( NodeCamDriver.cpp NodeCamFactory.cpp NodeCamDriver.h)
        add_to_hal_libraries( ${Node_LIBRARIES} )
        add_to_hal_libraries( ${ZeroMQPP_LIBRARIES} )
        add_to_hal_include_dirs( ${Node_INCLUDE_DIRS} ${GLOG_INCLUDE_DIRS} )
        add_to_hal_include_dirs( ${ZeroMQPP_INCLUDE_DIRS} )
    endif()
endif()
