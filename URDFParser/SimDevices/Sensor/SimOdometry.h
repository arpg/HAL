#ifndef SIMODOMETRY_H
#define SIMODOMETRY_H

// return x,y,th in 2D. This sensor measures how many truns (rotation) of the wheel and compute x,y,th.
class SimOdometry
{
   // request bullet to get the rotation of wheels and radius of wheels
   //        std::cout<<"rotation is "<<m_vehicle->getWheelInfo(1).m_rotation<<std::endl;
   // compute x,y

   // compute th
};



#endif // SIMODOMETRY_H
